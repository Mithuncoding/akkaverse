# ============================================================
# Akkaverse — Root environment template
# Copy to `.env` and fill in real values. NEVER commit `.env`.
# Per-app envs live in frontend/.env.local and backend/.env
# ============================================================

# ---------- PostgreSQL ----------
POSTGRES_USER=akkaverse
POSTGRES_PASSWORD=change_me
POSTGRES_DB=akkaverse
POSTGRES_PORT=5432

# ---------- Backend ----------
BACKEND_PORT=8000

# ---------- Frontend ----------
FRONTEND_PORT=3000
