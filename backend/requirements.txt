# ============================================================
# Akkaverse Backend — Python dependencies
# Pinned to minor versions for reproducible builds.
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Database ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Utilities ---
python-multipart==0.0.20      # file uploads (OCR later)
httpx==0.28.1                 # async HTTP for AI providers

# --- AI / RAG (wired up in Phase 1 chat + Phase 3) ---
# google-generativeai==0.8.3
# chromadb==0.5.23
# tiktoken==0.8.0
