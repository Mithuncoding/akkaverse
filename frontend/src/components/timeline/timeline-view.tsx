"use client";

import { JourneyView } from "@/components/timeline/journey-view";

/** The signature "Heritage Journey" — an immersive walk through 2,500 years. */
export function TimelineView() {
  return <JourneyView />;
}
