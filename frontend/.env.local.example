# ============================================================
# Akkaverse Frontend — environment template
# ============================================================
# Akkaverse runs entirely client-side. No API keys or backend
# are required to build, run, or deploy it on Vercel.
#
# This file is kept only as a placeholder for any future,
# optional integrations. NEXT_PUBLIC_* values are exposed to
# the browser by design — never put secrets here.
# ============================================================

# (no environment variables are required)
